apiVersion: eventing.knative.dev/v1alpha1
kind: Subscription
metadata:
  name: knative-foundations-subscription
spec:
  channel:
    apiVersion: eventing.knative.dev/v1alpha1
    kind: Channel
    name: knative-foundations-channel
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1alpha1
      kind: Service
      name: greeter-cli
